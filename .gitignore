__pycache__/
*.pyc

# Throwaway helper scripts kept at the repo root (push, audits, one-off checks).
# They are never part of the image, and this keeps a plain `git add .` from
# sweeping them into a commit.
/.*.sh
/.*.py
